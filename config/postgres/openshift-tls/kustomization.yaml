apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Chain: base → openshift (PGDATA patch) → openshift-tls (TLS patch)
resources:
  - ../openshift

patches:
  - path: tls-patch.yaml
    target:
      kind: Deployment
      name: postgres-deployment
